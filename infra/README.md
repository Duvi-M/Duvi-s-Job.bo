# Infrastructure

This folder is reserved for deployment, environment, and infrastructure assets as the MVP grows.

Current local infrastructure is defined in the root `docker-compose.yml` with PostgreSQL, backend, and frontend services.

